import { AddPrescriptionInput } from './add-prescription-input.model';

describe('AddPrescriptionInput', () => {
  it('should create an instance', () => {
    expect(new AddPrescriptionInput()).toBeTruthy();
  });
});
