1.open terminal
2. cd to ../client/
3. npm install
4. ng serve
4.5 **In some cases the client might complain about typescript version if so run "npm install --save-dev typescript@4.0.3"
then rerun ng serve again